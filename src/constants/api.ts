/** API path segments – used in app, config, and tests */
export const ApiPaths = {
  BASE: '/api/v1',
  DOCS: '/api-docs',
} as const;

/** Root route response message */
export const ROOT_MESSAGE = 'Express API';
